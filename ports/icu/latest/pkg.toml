name = "icu"
version = "latest"
summary = "Unicode and globalization support library"
license = "ICU"

deps = ["python"]

install_mode = "store"

[build]
system = "make"

[scripts]
build = "build.sh"
install = "install.sh"

[src]
type = "git"
url = "https://github.com/unicode-org/icu.git"

