name = "vulkan-headers"
version = "latest"
summary = "Vulkan header files and registry"
license = "Apache-2.0"

deps = []

install_mode = "store"

[build]
system = "make"

[scripts]
build = "build.sh"
install = "install.sh"

[src]
type = "git"
url = "https://github.com/KhronosGroup/Vulkan-Headers.git"
