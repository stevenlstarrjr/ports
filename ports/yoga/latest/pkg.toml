name = "yoga"
version = "latest"
summary = "Yoga embeddable layout engine"
license = "MIT"

deps = ["git", "googletest"]

install_mode = "store"

[build]
system = "make"

[scripts]
build = "build.sh"
install = "install.sh"

[src]
type = "git"
url = "https://github.com/facebook/yoga"
