name = "libdisplay-info"
version = "latest"
summary = "EDID and DisplayID parser library"
license = "MIT"

deps = ["hwdata"]

install_mode = "store"

[build]
system = "make"

[scripts]
build = "build.sh"
install = "install.sh"

[src]
type = "git"
url = "https://gitlab.freedesktop.org/emersion/libdisplay-info.git"
