name = "harfbuzz"
version = "latest"
summary = "Text shaping library"
license = "MIT"

deps = ["glib", "freetype", "icu"]

install_mode = "store"

[build]
system = "make"

[scripts]
build = "build.sh"
install = "install.sh"

[src]
type = "git"
url = "https://github.com/harfbuzz/harfbuzz.git"

