name = "vulkan-loader"
version = "latest"
summary = "Vulkan ICD loader"
license = "Apache-2.0"

deps = ["vulkan-headers", "wayland"]

install_mode = "store"

[build]
system = "make"

[scripts]
build = "build.sh"
install = "install.sh"

[src]
type = "git"
url = "https://github.com/KhronosGroup/Vulkan-Loader.git"
