name = "libpng"
version = "latest"
summary = "PNG reference library"
license = "libpng-2.0"

deps = ["zlib"]

install_mode = "store"

[build]
system = "make"

[scripts]
build = "build.sh"
install = "install.sh"

[src]
type = "git"
url = "https://github.com/pnggroup/libpng.git"

