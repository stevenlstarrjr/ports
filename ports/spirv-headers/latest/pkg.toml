name = "spirv-headers"
version = "latest"
summary = "SPIR-V headers and grammar files"
license = "Apache-2.0"

deps = []

install_mode = "store"

[build]
system = "make"

[scripts]
build = "build.sh"
install = "install.sh"

[src]
type = "git"
url = "https://github.com/KhronosGroup/SPIRV-Headers.git"
